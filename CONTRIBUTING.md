# Contributing

感谢你帮助改进 PaperPrism。

## 开发流程

1. Fork 仓库并从 `main` 创建功能分支。
2. 使用 `swift build` 或 `./scripts/build-app.sh` 验证 macOS 构建。
3. 保持界面浅色、克制、适合长时间阅读，并测试最小窗口尺寸。
4. 提交聚焦且可审查的 Pull Request，说明行为变化和验证方式。

## 安全与隐私

不得提交或附加：

- API Key、访问令牌、登录凭据或 `.env` 文件
- 第三方 Agent 的二进制文件、私有适配器或服务商专用配置
- 未获授权的论文全文、用户资料库或运行日志
- `.build`、分发压缩包和其他构建产物

请用虚构或已公开授权的数据制作测试样例。

## 许可

除非另有书面协议，你提交的贡献将以 AGPL-3.0-or-later 许可，并可由项目维护者作为双重许可版本的一部分提供。
